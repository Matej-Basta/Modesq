import React from "react";

export default function ProductDetails({product}) {

    const {name, description, picture, sizes, colors} = product;

    console.log(colors);

    return (
        <p>lala</p>
    );
}